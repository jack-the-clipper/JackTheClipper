package org.jacktheclipper.frontend.enums;

/**
 * Defines what type of content a source actually has. Depending on the type it needs to be
 * parsed differently.
 */
public enum ContentType {
    WebSite,
    Rss
}
